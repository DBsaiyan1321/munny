import React from "react"; 
import "./CalculatorPage.css";

const CalculatorPage = props => { 
    const [bonds, setBonds] = useState(state.calculator.bonds);
    const [midCap, setMidCap] = useState(state.calculator.midCap);
    const [largeCap, setLargeCap] = useState(state.calculator.largeCap);
    const [foreign, setForeign] = useState(state.calculator.foreign);
    const [smallCap, setSmallCap] = useState(state.calculator.smallCap);

    return (
        <div>CalculatorPage</div>
    )
};

export default CalculatorPage;